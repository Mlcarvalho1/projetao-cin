#pragma once

// Copie este arquivo para `secrets.h` (que está no .gitignore) e preencha.
// PSK: 32 bytes aleatórios. Mesma chave em TX e RX da mesma fazenda.
//   Gerar:  openssl rand -hex 32
#include <cstdint>

namespace aquasense::tx::secrets {

constexpr uint8_t PSK[32] = {
    0x00,0x00,0x00,0x00,0x00,0x00,0x00,0x00,
    0x00,0x00,0x00,0x00,0x00,0x00,0x00,0x00,
    0x00,0x00,0x00,0x00,0x00,0x00,0x00,0x00,
    0x00,0x00,0x00,0x00,0x00,0x00,0x00,0x00,
};

constexpr const char* WIFI_SSID = "your-ssid";
constexpr const char* WIFI_PASS = "your-password";

// IP do RX na rede local (descobrir após primeiro boot do RX).
constexpr const char* RX_HOST = "192.168.1.50";

} // namespace aquasense::tx::secrets
